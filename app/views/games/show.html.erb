<%= turbo_stream_from "player_#{@player.id}" %>

<!-- Menu Hamburguer -->
<%= render 'shared/hamburger_menu', game: @game, player: @player %>

<div id="game_content" class="flex-1 flex flex-col min-h-screen">
  <%= render 'games/game_content', game: @game, player: @player %>
</div>

<%= render 'shared/rules_modal' %>
<%= render 'shared/roles_modal' %>

